{
  "name": "teachassist-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step for functions, Netlify will handle it'"
  },
  "dependencies": {
    "@netlify/functions": "^4.2.7",
    "@types/cors": "^2.8.19",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "node-fetch-cookies": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
